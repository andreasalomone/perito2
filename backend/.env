# --- Google Cloud Project ---
GOOGLE_CLOUD_PROJECT=perito-479708
GOOGLE_CLOUD_REGION=europe-west1
# (Or 'europe-west1' if you chose Belgium. MUST match your Queue/DB region)

# --- Cloud SQL (Database) ---
# Format: project-id:region:instance-name
# Find this in GCP Console > SQL > Overview > "Connection name"
CLOUD_SQL_CONNECTION_NAME=perito-479708:europe-west1:robotperizia-db

DB_USER=report_user
# The password you generated in Step 3
DB_PASS=uJ_M(Z[ABzz+AtdT
DB_NAME=perizia_db

# --- Storage (GCS) ---
# The bucket name you created in Step 2
STORAGE_BUCKET_NAME=robotperizia-uploads-roberto

# --- Cloud Tasks (Queue) ---
# Format: projects/{project}/locations/{region}/queues/{queue_name}
CLOUD_TASKS_QUEUE_PATH=projects/perito/locations/europe-west1/queues/report-processing-queue

# --- AI (Gemini) ---
# Get this from Google AI Studio (aistudio.google.com)
GEMINI_API_KEY=AIzaSyC0-_LEguVy2fJKxurof6h8RIfc0_75gxE

# --- Local Development Only ---
# Point this to your downloaded Service Account JSON key for local testing.
# On Cloud Run (Production), comment this out (it uses auto-discovery).
GOOGLE_APPLICATION_CREDENTIALS="service-account.json"

# Force synchronous execution locally (Bypass Cloud Tasks)
RUN_LOCALLY=True
